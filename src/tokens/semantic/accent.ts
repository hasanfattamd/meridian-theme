import { blue, purple } from '../primitives/index.js';

/**
 * Semantic tokens for brand and interactive accents.
 */
export const accent = {
  primary: blue[500],
  secondary: purple[500],
  hover: blue[400],
  active: blue[600],
} as const;
